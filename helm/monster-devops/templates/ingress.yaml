apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: monster-app-ingress

spec:
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: monster-app-service
                port:
                  number: {{ .Values.service.port }}
