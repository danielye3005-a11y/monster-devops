apiVersion: v1
kind: Service

metadata:
  name: monster-app-service
  labels:
    app: monster-app

spec:
  selector:
    app: monster-app

  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}

  type: ClusterIP
